name: Continuous Integration

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # Call the reusable workflow
  run-linting:
    uses: ./.github/workflows/workflow_call.yml
    with:
      node-version: '22' # Passing the input parameter
